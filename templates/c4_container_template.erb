<%# auto-doc C4 container diagram template %>
<%# Available: title, modules, data_flows, generated_at %>

> Container Diagram: <%= title %>
> Generated by auto-doc on <%= generated_at %>

graph TB
<% modules.each do |mod| %>
    <%= mod[:name].gsub(/\s+/, '_') %>[<b><%= mod[:name] %></b><br/><%= mod[:description] %>]
<% end %>

<% data_flows.each do |flow| %>
    <%= flow[:from].gsub(/\s+/, '_') %> -->|<%= flow[:label] %>| <%= flow[:to].gsub(/\s+/, '_') %>
<% end %>
