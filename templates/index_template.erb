<%# auto-doc template for INDEX.md %>
# INDEX: <%= dir_name %>

> **Generated by auto-doc v<%= AutoDoc::VERSION %>** on <%= generated_at %>  
> Files: <%= files.size %> | Symbols: <%= symbols.size %> | Documentation coverage: <%= coverage_pct %>% 

## Files

| # | File | Classes | Modules | Methods | Documented |
|---|------|---------|---------|---------|------------|
<% if files.any? %>
<% files.each_with_index do |f, idx| %>
| <%= idx + 1 %> | <%= f[:name] %> | <%= f[:classes] || 0 %> | <%= f[:modules] || 0 %> | <%= f[:methods] || 0 %> | <%= f[:documented] ? "✅" : "❌" %> |
<% end %>
<% else %>
| _No Ruby files found_ | — | — | — | — | — |
<% end %>

## Symbols

| Symbol | Type | File | Line | Documented |
|--------|------|------|------|------------|
<% if symbols.any? %>
<% symbols.each do |sym| %>
| <%= sym[:name] %> | <%= sym[:type] %> | <%= sym[:file] %> | <%= sym[:line] %> | <%= sym[:doc] ? "✅" : "❌" %> |
<% end %>
<% else %>
| _No symbols found_ | — | — | — | — |
<% end %>

## Dependencies

| From | Type | To |
|------|------|----|
<% if dependencies.any? %>
<% dependencies.each do |dep| %>
| <%= dep[:from] %> | <%= dep[:type] %> | <%= dep[:to] %> |
<% end %>
<% else %>
| _No dependencies detected_ | — | — |
<% end %>

<% if cross_references %>
## Cross-References

<% if cross_references[:parent] %>
- **Parent:** [<%= cross_references[:parent][:name] %>](<%= cross_references[:parent][:path] %>)
<% end %>
<% if cross_references[:children] && cross_references[:children].any? %>
- **Modules:**
<% cross_references[:children].each do |child| %>
  - [<%= child[:name] %>](<%= child[:path] %>)
<% end %>
<% end %>
<% if cross_references[:siblings] && cross_references[:siblings].any? %>
- **Siblings:**
<% cross_references[:siblings].each do |sib| %>
  - [<%= sib[:name] %>](<%= sib[:path] %>)
<% end %>
<% end %>
<% end %>

---

*Generated by auto-doc. Review and commit these drafts as your documentation.*
