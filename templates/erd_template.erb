<%# auto-doc entity-relationship diagram template %>
<%# Available: title, tables, relationships, generated_at %>

> Entity-Relationship Diagram: <%= title %>
> Generated by auto-doc on <%= generated_at %>

erDiagram
<% tables.each do |table| %>
    <%= table[:name] %> {
<% table[:columns].each do |col| %>
        <%= col[:type] %> <%= col[:name] %><%= col[:pk] ? " PK" : "" %><%= col[:fk] ? " FK" : "" %><%= col[:null] == false ? " NOT NULL" : "" %>
<% end %>
    }
<% end %>

<% relationships.each do |rel| %>
    <%= rel[:from] %> <%= rel[:cardinality_from] || "||" %>--<%= rel[:cardinality_to] || "o{" %> <%= rel[:to] %> : <%= rel[:label] || "has" %>
<% end %>
