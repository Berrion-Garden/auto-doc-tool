<%# auto-doc architecture document template %>
<%# Available: title, overview, architecture_style, modules, data_flows, design_decisions, diagram_links, generated_at %>

# <%= title %>

> Generated by auto-doc on <%= generated_at %>

## System Overview

<%= overview %>

## Architecture Style

<%= architecture_style %>

## Module Map

| Module | Responsibility |
|--------|---------------|
<% modules.each do |mod| %>
| <%= mod[:name] %> | <%= mod[:responsibility] %> |
<% end %>

## Data Flow

<% if data_flows.any? %>
| From | To | Description |
|------|----|-------------|
<% data_flows.each do |flow| %>
| <%= flow[:from] %> | <%= flow[:to] %> | <%= flow[:description] %> |
<% end %>
<% else %>
_No data flows defined._
<% end %>

## Design Decisions

<% if design_decisions.any? %>
<% design_decisions.each do |decision| %>
### <%= decision[:title] %>

<%= decision[:body] %>

<% end %>
<% else %>
_No design decisions recorded._
<% end %>

## Diagrams

<% if diagram_links.any? %>
<% diagram_links.each do |link| %>
<% if link.is_a?(Hash) %>
- [<%= link[:title] %>](<%= link[:path] %>)
<% else %>
- [<%= File.basename(link.to_s, '.mmd') %>](<%= link %>)
<% end %>
<% end %>
<% else %>
_No diagrams generated._
<% end %>

---

*Generated by auto-doc. Review and commit as architecture documentation.*
