<%# auto-doc C4 context diagram template %>
<%# Available: title, external_systems, internal_system, generated_at %>

> System Context Diagram: <%= title %>
> Generated by auto-doc on <%= generated_at %>

graph TB
    subgraph external["External Systems"]
<% external_systems.each do |sys| %>
        <%= sys[:name].gsub(/\s+/, '_') %>[<%= sys[:name] %>]
<% end %>
    end

    subgraph system["<%= internal_system[:name] %>"]
        <%= internal_system[:name].gsub(/\s+/, '_') %>[<%= internal_system[:name] %>]
    end

<% external_systems.each do |sys| %>
    <%= sys[:name].gsub(/\s+/, '_') %> -->|<%= sys[:interaction] %>| <%= internal_system[:name].gsub(/\s+/, '_') %>
<% end %>
