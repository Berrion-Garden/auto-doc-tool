<%# auto-doc template for project-root AGENTS.md %>
# <%= project_name %> — Project Overview

> **Generated by auto-doc v<%= AutoDoc::VERSION %>** on <%= generated_at %>  
> Modules: <%= module_count %> | Source files: <%= total_files %>

## Project Overview

<%= overview_text %>

## Directory Structure

```
<%= tree_text %>
```

## Technology Stack

<%= tech_stack_text %>

## Architecture

<%= architecture_text %>

## Build & Test Commands

| Command | Description |
|---------|-------------|
| `bundle exec rspec` | Run the full test suite |
| `ruby -I lib exe/auto-doc generate .` | Generate documentation |
| `ruby -I lib exe/auto-doc audit` | Run documentation coverage audit |
| `ruby -I lib exe/auto-doc serve` | Start the documentation server |

## Key Conventions

<%= conventions_text %>

## External Dependencies

| Dependency | Type |
|------------|------|
<% external_deps.each do |dep| %>
| <%= dep[:name] %> | <%= dep[:type] %> |
<% end %>

<% if external_deps.empty? %>
| _No external dependencies detected_ | — |
<% end %>

---

*Generated by auto-doc. Review and commit these drafts as your documentation.*
