<%# auto-doc DAG diagram template %>
<%# Available: title, graph_nodes, graph_edges %>

> Dependency Graph: <%= title %>
> Generated by auto-doc on <%= generated_at %>

graph TB
<% graph_nodes.each do |node| %>
    <%= node.gsub(/\s+/, '_') %>[<%= node %>]
<% end %>

<% graph_edges.each do |edge| %>
    <%= edge[:from].gsub(/\s+/, '_') %> -->|<%= edge[:type] %>| <%= edge[:to].gsub(/\s+/, '_') %>
<% end %>
