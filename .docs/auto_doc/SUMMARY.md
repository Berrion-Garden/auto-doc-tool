
# SUMMARY: auto_doc

> **Generated by auto-doc v1.0.0** on 2026-07-15 22:11:50 PDT

## Purpose

The `AutoDoc` module is an automated documentation generation system designed primarily for Ruby and Ruby on Rails applications. Its purpose is to analyze a codebase, extract structural and semantic information, and produce a comprehensive, standardized set of documentation artifacts without manual intervention.

**Key Components:**
- **Analyzer Layer:** Parses Ruby source code, YARD documentation, database schemas, and model associations to map dependencies, imports, and data relationships. Includes services for tracking file changes and identifying orphaned or stale files.
- **Transformer Layer:** Converts raw analysis data into structured, machine-readable formats such as class hierarchies, container data flows, entity-relationship models, and graph-based dependency networks.
- **Generator Layer:** Produces multiple documentation outputs including READMEs, architecture overviews, class diagrams, ERDs, C4 diagrams, vector embeddings for semantic search, and summary indices.
- **LLM Integration:** Interfaces with large language models to generate human-readable summaries, augment content, and power an intelligent query service for interactive documentation exploration.
- **Orchestrator:** Manages a step-based execution pipeline that coordinates analysis, transformation, generation, and indexing tasks in a deterministic, configurable sequence.
- **Quality & Reporting:** Includes completeness checkers and audit reporters to validate documentation coverage, track metrics, and ensure consistency across generated artifacts.
- **Infrastructure & Utilities:** Provides a CLI interface, a web server for interactive access, configuration management, caching, search indexing, file tree utilities, and Markdown/output formatting helpers.

In summary, `AutoDoc` functions as an end-to-end documentation pipeline that bridges static code analysis, AI-assisted content generation, and structured output creation to keep application documentation accurate, searchable, and automatically synchronized with the codebase.

## Key Components


| Component | Type | Summary |
|-----------|------|---------|

| auto_doc | module | Based on the provided metadata, the `AutoDoc` project implements a modular, pipeline-driven architecture for automated software documentation generation. The components are logically grouped into functional layers that enforce a strict separation of concerns.

### **Architectural Pattern**
The system follows an **ETL-inspired pipeline model** (Extract, Transform, Generate) with cross-cutting services for AI augmentation, indexing, and validation. The `orchestrator/pipeline.rb` acts as the central workflow router, coordinating decoupled analysis, transformation, generation, and reporting stages.

### **Component Relationships & Dependencies**
- **Orchestration Layer:** `orchestrator/pipeline.rb` and `orchestrator/base_step.rb` manage execution flow. Concrete steps (`readme_step.rb`, `architecture_step.rb`, `diagram_step.rb`, `manifest_step.rb`, etc.) depend on the base step and coordinate sequencing, metrics, and step-specific logic.
- **Analysis Layer:** `analyzer/analysis_pipeline.rb` serves as the primary extraction engine. It depends on specialized parsers (`source_parser.rb`, `schema_parser.rb`, `yard_reader.rb`, `import_extractor.rb`) and analysis services (`diff_service.rb`, `orphans_service.rb`, `model_association_parser.rb`) to parse Ruby codebases and extract structural metadata.
- **Transformation & Indexing Layer:** `transformer/` components consume raw analysis data to build domain-specific models (`class_hierarchy_builder.rb`, `graph_data_builder.rb`, `erd_relationship_builder.rb`, `container_data_flow_builder.rb`, `files_data_builder.rb`). `documentation_index.rb` and `search_service.rb` manage persistent indexing and retrieval.
- **AI/LLM Layer:** `llm/client.rb` abstracts external model communication, while `llm/summarizer.rb` processes extracted context. `agent_query_service.rb` interfaces with the LLM for documentation synthesis and agent-driven queries, acting as a shared dependency across multiple pipeline steps.
- **Generation Layer:** `generator/` components consume transformed and LLM-augmented data to produce final assets. Diagram generators (`c4_diagram_generator.rb`, `class_diagram_generator.rb`, `erd_generator.rb`, `vector_generator.rb`) depend on graph/hierarchy builders, while markdown/text generators (`readme_generator.rb`, `architecture_generator.rb`, `summary_generator.rb`, `index_generator.rb`, `agents_md_generator.rb`) consume narrative or structured data. `diagram_generator.rb` likely serves as a base or factory.
- **Validation & Reporting:** `reporter/completeness_checker.rb` and `reporter/audit_reporter.rb` depend on both analysis outputs and generated assets to verify coverage and produce audit reports.
- **Infrastructure & Utilities:** Root components (`cli.rb`, `server.rb`, `config.rb`) handle entry points, HTTP serving, and configuration. `utils/` provides cross-cutting concerns (`yaml_config_loader.rb`, `file_tree_builder.rb`, `markdown_helper.rb`, `output_formatter.rb`, `timestamp_tracker.rb`). `version.rb` exposes build metadata.

### **Data Flow Sequence**
1. **Input & Configuration:** `cli.rb` or `server.rb` receives a target codebase, loads settings via `config.rb` and `yaml_config_loader.rb`, and triggers the orchestrator.
2. **Extraction:** The orchestrator invokes `analyzer/analysis_pipeline.rb`. Parsers scan files to extract ASTs, dependencies, schemas, and YARD comments. Diff and orphan services identify stale or unused code elements.
3. **Transformation & Indexing:** Raw metadata flows into `transformer/` builders, which construct hierarchical, graph, and data-flow representations. This enriched dataset is indexed by `documentation_index.rb` and exposed through `search_service.rb` for downstream consumption.
4. **AI Augmentation:** Contextual data is routed to `llm/summarizer.rb` and `agent_query_service.rb` to generate architectural narratives, code summaries, and agent prompts.
5. **Output Generation:** The orchestrator dispatches specific generator steps. Processed data feeds into diagram builders (C4, ERD, class, vector) and markdown generators (readme, architecture, summaries, index).
6. **Validation & Delivery:** `reporter/` components audit generated assets against analysis data for completeness. Final outputs are formatted via `output_formatter.rb` and persisted or served through the CLI or server.

### **Key Architectural Notes**
- **Dependency Direction:** Strictly bottom-up. Utilities → Analysis → Transformation → LLM/Generation → Reporting. The orchestrator sits at the top as a decoupled director.
- **Coupling Strategy:** The pipeline uses step-based composition (`base_step.rb`), enabling parallel execution, retry logic, and modular swapping of analysis/generation strategies without touching core infrastructure.
- **Cross-Cutting Services:** LLM clients, indexing, and search services are designed as shared dependencies, allowing any step or generator to inject AI or retrieval capabilities on demand.
- **Extensibility:** New documentation formats or analysis rules can be added by implementing new generators or analyzer parsers without altering the orchestration core. |



## Architecture Pattern

### Architecture Summary

**auto_doc** is a modular, pipeline-based documentation generation toolkit designed to automate the creation of technical documentation from source code. The architecture follows a separation of concerns pattern, dividing functionality into distinct phases: analysis, transformation, generation, and orchestration. It leverages static analysis of code and schemas, enriches data via graph and hierarchy builders, integrates Large Language Models (LLMs) for summarization and enrichment, and produces diverse documentation artifacts including diagrams, Markdown files, and indexes.

### Major Modules

*   **Orchestrator:** The central workflow engine that manages the execution pipeline. It defines and sequences discrete steps (e.g., analysis, diagram generation, README creation) and handles step coordination via a base step abstraction.
*   **Analyzer:** Responsible for parsing source code and extracting metadata. It handles YARD comments, schema definitions, imports, model associations, and diff tracking. It also manages an analysis cache to optimize repeated runs.
*   **Transformer:** Bridges the gap between raw analysis data and visualization requirements. It builds intermediate data structures such as class hierarchies, container data flows, ERD relationships, and graph data representations.
*   **Generator:** Produces the final documentation artifacts. This module includes specialized generators for various formats, including C4 diagrams, class diagrams, ERDs, architecture maps, vector data, and Markdown content (ReadME, Index, Agents).
*   **LLM Integration:** Provides interfaces for Large Language Model interactions. It includes a client abstraction and a summarizer component, likely used to enhance documentation with AI-generated summaries or insights.
*   **Reporter:** Handles quality assurance and auditing. It performs completeness checks and generates audit reports to validate the documentation output.
*   **Search & Query:** Includes services for searching documentation and handling agent queries, suggesting the tool may support querying the generated docs or interacting with AI agents.
*   **Interfaces:** Entry points for the tool, including a CLI for command-line execution and a Server module for HTTP-based interactions.
*   **Configuration & Utils:** Core utilities for loading YAML configuration, tracking timestamps, formatting output, managing file trees, and handling versioning.

### Module Relationships

1.  **Workflow Orchestration:** The **Orchestrator** drives the process, invoking the **Analyzer** to extract code metadata, followed by the **Transformer** to build structural models, and finally the **Generators** to produce output.
2.  **Data Flow:**
    *   The **Analyzer** feeds raw metadata into the **Transformer**.
    *   The **Transformer** enriches this data into graphs and hierarchies, which are consumed by specific **Generator** modules (e.g., graph data for `diagram_generator`, class hierarchy for `class_diagram_generator`).
3.  **AI Integration:** The **LLM** module is likely called by the **Orchestrator** or specific **Generators** to perform summarization or content generation tasks, bridging static analysis with dynamic AI capabilities.
4.  **Feedback & Validation:** The **Reporter** module sits at the end of the pipeline, consuming the generated documentation to verify completeness and generate audit reports.
5.  **Caching & Optimization:** The **Analyzer** utilizes an **Analysis Cache** to avoid redundant processing, while the **Orchestrator** manages state across steps.
6.  **Central Index:** A **Documentation Index** likely serves as a central registry or data structure aggregating information across the analysis and generation phases.

## Dependencies Overview


| Name | Type |
|------|------|

| ../auto_doc/server | local |

| erb | stdlib/gem |

| fileutils | stdlib/gem |

| json | stdlib/gem |

| llm/client | path |

| llm/summarizer | path |

| MetricsHelper | stdlib/gem |

| net/http | path |

| pathname | stdlib/gem |

| ripper | stdlib/gem |

| set | stdlib/gem |

| shellwords | stdlib/gem |

| sinatra/base | path |

| TemplateHelper | stdlib/gem |

| thor | stdlib/gem |

| time | stdlib/gem |

| transformer | stdlib/gem |

| transformer/class_hierarchy_builder | path |

| transformer/container_data_flow_builder | path |

| transformer/erd_relationship_builder | path |

| transformer/files_data_builder | path |

| transformer/graph_data_builder | path |

| uri | stdlib/gem |

| utils/markdown_helper | path |

| utils/yaml_config_loader | path |

| yaml | stdlib/gem |

| yard | stdlib/gem |



## Related Documents

- [INDEX.md](INDEX.md) — Detailed file and symbol index
- [AGENTS.md](AGENTS.md) — Agent-oriented module documentation


---

*Generated by auto-doc. Review and commit these drafts as your documentation.*
