
# SUMMARY: auto_doc

> **Generated by auto-doc v1.0.0** on 2026-07-16 11:29:31 PDT

## Purpose

### **Module Purpose**
The `auto_doc` module is an automated technical documentation generation tool designed to analyze a software codebase, extract architectural and semantic insights, and produce comprehensive, up-to-date documentation artifacts. It combines static code analysis, structured data transformation, and LLM-driven enrichment to eliminate manual documentation overhead while maintaining engineering accuracy.

### **Key Components**

| Component Group | Description |
|----------------|-------------|
| **Analysis & Parsing Engine (`analyzer/`)** | Scans source files, schemas, YARD comments, and model definitions to extract imports, dependencies, associations, and orphaned resources. Includes `AnalysisCache` and `DiffService` to optimize incremental runs and track changes. |
| **Data Transformation Layer (`transformer/`)** | Converts raw parsed metadata into structured, queryable formats. Builds class hierarchies, data flow maps, ERD relationships, and graph-based project representations for downstream consumption. |
| **LLM Integration (`llm/` & `agent_query_service.rb`)** | Interfaces with external AI services for prompt construction, response parsing, text summarization, and agent-driven querying. Enables AI-enhanced documentation insights and dynamic content generation. |
| **Documentation Generators (`generator/`)** | Produces multiple output formats including READMEs, architecture overviews, C4/class diagrams, ER diagrams, agent documentation, vector indexes, summary reports, and map files. Uses `TemplateHelper` for consistent formatting. |
| **Pipeline Orchestration (`orchestrator/`)** | Manages the end-to-end execution workflow through a step-based pipeline architecture. Coordinates data flow between analysis, transformation, generation, and indexing phases via `BaseStep` and specialized step classes. |
| **Reporting & Quality Assurance (`reporter/`)** | Provides `AuditReporter` and `CompletenessChecker` to validate documentation coverage, track generation metrics, and ensure alignment with project standards. |
| **Core Infrastructure & Utilities** | Includes CLI entry points, configuration management, a local server interface, YAML config loading, file tree building, markdown/output formatting, timestamp tracking, a documentation index/search service, and error handling definitions. |

### **Workflow Summary**
The module operates as a structured pipeline: source code is parsed and analyzed → transformed into structural/graph data → orchestrated through configurable generation steps → optionally enriched via LLM agents → output as formatted documentation artifacts. Caching, diff tracking, and completeness auditing ensure efficient, repeatable, and high-quality documentation generation tailored to modern software projects.

## Key Components


| Component | Type | Summary |
|-----------|------|---------|

| auto_doc | module | Based on the provided component metadata, `AutoDoc` follows a modular, multi-stage architecture designed for automated software documentation generation. The system is organized into distinct functional layers that process code artifacts through ingestion, transformation, AI enhancement, rendering, and validation.

### **Architectural Overview & Component Relationships**
- **Orchestration Hub:** The `orchestrator` module acts as the central workflow manager. It uses a pipeline pattern (`orchestrator/pipeline.rb`) to sequence execution steps, each represented as a discrete class (e.g., `architecture_step`, `diagram_step`, `readme_step`). Steps inherit from `base_step` and coordinate with lower-level services without direct coupling.
- **Analysis & Extraction Layer:** The `analyzer` module handles raw code and documentation ingestion. Components like `source_parser`, `model_association_parser`, `diff_service`, and `yard_reader` extract structure, dependencies, and existing documentation. Results are persisted in `analysis_cache` to avoid redundant processing.
- **Transformation & Modeling Layer:** The `transformer` module bridges analysis and generation. It converts parsed data into structured models using builders for class hierarchies, container data flows, ERD relationships, and graph structures (`graph_data_builder`, `class_hierarchy_builder`, etc.).
- **AI/LLM Integration:** The `llm` module abstracts external large language model interactions. It provides a unified interface for client communication, prompt assembly, response parsing, and text summarization. Orchestrator steps delegate content generation or enrichment tasks to this layer.
- **Generation & Rendering Layer:** The `generator` module produces final documentation artifacts. It consumes transformed models and LLM outputs to render Markdown, architectural diagrams (C4, class, ERD), vector embeddings, and index/summary files. It relies on `template_helper` for consistent formatting.
- **Quality & Reporting Layer:** The `reporter` module validates output quality through `completeness_checker` and `audit_reporter`, ensuring generated documentation meets project standards.
- **Supporting Infrastructure:** 
  - **Entry Points:** `cli.rb` and `server.rb` expose the tool as a command-line or web interface.
  - **Configuration:** `config.rb` and `yaml_config_loader` manage settings.
  - **Output Management:** `documentation_index.rb` and `search_service.rb` organize and enable retrieval of generated files.
  - **Cross-Cutting Utilities:** `utils/` provides shared helpers for markdown formatting, file tree construction, timestamp tracking, and output styling.

### **Dependency Direction**
Dependencies flow upward through abstraction layers:
`utils` → `analyzer` → `transformer` → `llm` → `orchestrator` → `generator` → `reporter`
The orchestrator depends on all service layers to execute pipeline steps, while generators depend on transformed models and LLM outputs. No component depends on the orchestrator or generator layers, maintaining a clean separation of concerns.

### **Data Flow**
1. **Trigger & Configuration:** CLI or server invokes the orchestrator, which loads project configuration and initializes the pipeline.
2. **Ingestion:** The analyzer scans source files, schemas, and documentation, producing raw AST/metadata and change diffs. Results are cached.
3. **Model Construction:** The transformer ingests analysis output and builds relational/graph models (class trees, data flows, dependency graphs).
4. **AI Enhancement:** Orchestrator steps route relevant model data to the `llm` module for prompt generation, response parsing, and summarization.
5. **Artifact Rendering:** The generator consumes enriched models and AI responses, applying templates to produce Markdown, diagrams, vector indexes, and READMEs.
6. **Validation & Indexing:** The reporter audits completeness and logs findings. Final artifacts are organized via the documentation index and exposed through the search service, with timestamps and formatting applied by utilities.

This architecture promotes extensibility, caching efficiency, and clear separation between data extraction, modeling, AI interaction, and output rendering, making it well-suited for automated, repeatable documentation generation workflows. |



## Architecture Pattern

### Overall Architecture
`auto_doc` follows a **modular, pipeline-driven architecture** designed to automate end-to-end software documentation generation. The system processes a codebase through a sequenced workflow managed by a central orchestrator. It combines static code analysis, structural data transformation, and AI-assisted generation to produce comprehensive, multi-format documentation artifacts. All components are encapsulated within the `AutoDoc` Ruby namespace, adhering to a clean separation of concerns between ingestion, processing, generation, and validation phases.

### Major Modules & Responsibilities
- **CLI & Server:** Entry points that expose the tool via command-line interface or as an HTTP service. They route user requests to the core processing pipeline.
- **Orchestrator:** The workflow engine that defines, sequences, and executes processing steps. It manages step lifecycle, tracks execution metrics, and coordinates cross-module data handoffs.
- **Analyzer:** Responsible for deep code inspection. Parses source files, schemas, imports, and model associations. Handles caching, change detection (diffs), orphan discovery, and YARD documentation extraction to build a foundational code understanding.
- **Transformer:** Bridges raw analysis and final output. Converts parsed metadata into structured, query-ready models such as class hierarchies, container data flows, ERD relationships, and graph/network structures.
- **LLM Integration:** AI augmentation layer. Manages client communication, prompt construction, response parsing, and intelligent summarization to enrich documentation with context-aware insights.
- **Generator:** The output rendering layer. Produces final documentation assets including Markdown files, C4/Class/ERD diagrams, architecture maps, indexes, summaries, and vector embeddings.
- **Reporter:** Quality assurance module. Validates documentation completeness against configurable standards and generates audit reports for compliance and coverage tracking.
- **Search & Query Services:** Enable full-text or semantic retrieval across generated documentation and code artifacts.
- **Utilities & Core Infrastructure:** Shared components including configuration management, YAML loaders, file tree builders, markdown helpers, output formatters, timestamp tracking, error definitions, versioning, and E2E testing runners.

### Module Relationships & Data Flow
The system operates on a **linear pipeline with feedback/validation loops**:
1. The **CLI/Server** initiates execution and passes control to the **Orchestrator**.
2. The **Orchestrator** triggers the **Analyzer** to ingest and parse the codebase, caching results to avoid redundant work.
3. Raw analysis data flows into the **Transformer**, which structures it into graphs, hierarchies, and data flow models.
4. The transformed data is optionally passed to the **LLM module** for AI-driven enrichment or summarization before reaching the **Generator**.
5. The **Generator** renders the enriched data into the target documentation formats.
6. Finally, the **Reporter** evaluates the output against completeness criteria, while the **Search/Query services** index the results for runtime retrieval.
7. All modules depend on shared **Utilities** and **Configuration**, ensuring consistent formatting, path resolution, and environment handling. Errors are centralized for predictable exception handling across the pipeline.

## Dependencies Overview


| Name | Type |
|------|------|

| ../auto_doc/server | local |

| erb | stdlib/gem |

| fileutils | stdlib/gem |

| json | stdlib/gem |

| llm/client | path |

| llm/prompt_builder | path |

| llm/response_parser | path |

| llm/summarizer | path |

| MetricsHelper | stdlib/gem |

| net/http | path |

| pathname | stdlib/gem |

| ripper | stdlib/gem |

| set | stdlib/gem |

| shellwords | stdlib/gem |

| sinatra/base | path |

| TemplateHelper | stdlib/gem |

| thor | stdlib/gem |

| time | stdlib/gem |

| transformer | stdlib/gem |

| transformer/class_hierarchy_builder | path |

| transformer/container_data_flow_builder | path |

| transformer/erd_relationship_builder | path |

| transformer/files_data_builder | path |

| transformer/graph_data_builder | path |

| uri | stdlib/gem |

| utils/markdown_helper | path |

| utils/yaml_config_loader | path |

| yaml | stdlib/gem |

| yard | stdlib/gem |



## Related Documents

- [INDEX.md](INDEX.md) — Detailed file and symbol index
- [AGENTS.md](AGENTS.md) — Agent-oriented module documentation


---

*Generated by auto-doc. Review and commit these drafts as your documentation.*
