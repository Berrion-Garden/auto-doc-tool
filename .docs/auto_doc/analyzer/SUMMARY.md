
# SUMMARY: analyzer

> **Generated by auto-doc v1.0.0** on 2026-07-16 13:08:05 PDT

## Purpose

### Purpose
The `analyzer` module acts as the core intelligence engine for the `AutoDoc` toolkit. Its primary purpose is to automatically inspect, parse, and evaluate a codebase (optimized for Ruby/Ruby on Rails projects) to generate, update, and validate technical documentation without manual intervention.

### Core Functionality
The module systematically extracts structural, relational, and documented information from source files, database schemas, and existing code comments. It orchestrates these extractions through a structured workflow, caches results for performance, tracks code changes to scope documentation updates, and identifies undocumented or disconnected code elements to highlight documentation gaps.

### Key Components
Grouped by responsibility, the module consists of the following:

* **Orchestration & Performance**
  * `analysis_pipeline.rb`: Coordinates the multi-step analysis workflow, ensuring components run in the correct sequence.
  * `analysis_cache.rb`: Stores parsed results and intermediate data to prevent redundant processing and improve performance.

* **Code & Data Structure Parsing**
  * `source_parser.rb`: Reads and interprets raw source code files to extract classes, methods, and syntax structures.
  * `generic_scanner.rb`: Performs broad pattern matching and symbol discovery across the codebase.
  * `schema_parser.rb`: Extracts database structure, tables, and column definitions from schema files or migrations.
  * `model_association_parser.rb`: Maps model relationships (e.g., one-to-many, belongs-to) to document data dependencies.

* **Dependency & Documentation Extraction**
  * `import_extractor.rb`: Catalogs module/library dependencies and `require`/`include` statements to map external connections.
  * `yard_reader.rb`: Ingests existing YARD documentation comments to preserve and cross-reference human-written docs.

* **Change & Gap Analysis**
  * `diff_service.rb`: Compares current code state against previous versions to identify modified elements that require documentation updates.
  * `orphans_service.rb`: Detects undocumented code elements or broken documentation links, highlighting areas that need attention.

### Summary
Together, these components form a cohesive analysis layer that transforms raw code and existing documentation into a structured, up-to-date documentation map. The module enables `AutoDoc` to automate documentation generation, track documentation debt, and ensure documentation accuracy as the codebase evolves.

## Key Components


| Component | Type | Summary |
|-----------|------|---------|

| analyzer | module | Based on the provided component metadata, here is a concise architectural summary:

### 🔗 Component Relationships
The `AutoDoc::Analyzer` namespace is structured into four logical layers that form a modular analysis pipeline:
- **Orchestration Layer:** `analysis_pipeline.rb` serves as the central coordinator, managing workflow execution, stage sequencing, and error handling.
- **Ingestion & Parsing Layer:** `yard_reader.rb`, `source_parser.rb`, and `generic_scanner.rb` handle initial codebase ingestion, AST/syntax parsing, and YARD documentation extraction.
- **Specialized Analysis Layer:** `import_extractor.rb`, `model_association_parser.rb`, and `schema_parser.rb` transform raw parsed data into structured representations of code dependencies, ORM associations, and database/API schemas.
- **Validation & Quality Services:** `diff_service.rb` and `orphans_service.rb` consume analysis results to detect code/documentation drift and identify unreachable or undocumented elements.
- **Cross-Cutting Utility:** `analysis_cache.rb` is a shared state management component used across multiple stages to store/retrieve intermediate results and optimize performance.

### 📦 Dependencies
- `analysis_pipeline.rb` has no internal dependencies but orchestrates all other modules.
- Ingestion components (`source_parser`, `yard_reader`, `generic_scanner`) are foundational; specialized analyzers and services logically depend on their outputs.
- Specialized parsers (`import_extractor`, `model_association_parser`, `schema_parser`) depend on base parsing utilities and feed into validation services.
- `diff_service.rb` and `orphans_service.rb` depend on the structured outputs from the parsing/analysis layer.
- All analysis, parsing, and service components interact with `analysis_cache.rb` for read/write operations, creating a highly decoupled dependency pattern centered on the cache for performance.

### 🌊 Data Flow
1. **Input & Ingestion:** Source files, imports, and inline documentation are scanned and parsed by `generic_scanner.rb`, `source_parser.rb`, and `yard_reader.rb`, producing raw AST/syntax nodes and documentation fragments.
2. **Extraction & Structuring:** Parsed data is routed to specialized processors (`import_extractor.rb`, `model_association_parser.rb`, `schema_parser.rb`), which resolve relationships and build a unified representation of the codebase's architecture.
3. **Validation & Comparison:** The structured model is passed to `diff_service.rb` (to compute changes against prior states) and `orphans_service.rb` (to flag missing documentation or broken references).
4. **Orchestration & Caching:** Throughout the pipeline, `analysis_cache.rb` intercepts data reads/writes to avoid redundant parsing or computation. The `analysis_pipeline.rb` monitors stage completion, manages cache invalidation, and aggregates final results for downstream consumption.

This architecture follows a staged pipeline pattern with strong separation of concerns, relying on a shared cache and pipeline orchestrator to maintain modularity, scalability, and efficient re-analysis. |



## Architecture Pattern

### Overall Architecture
The `analyzer` component follows a **modular, pipeline-driven architecture** designed for static code analysis and documentation intelligence. It decouples code ingestion, semantic parsing, differential analysis, and result caching into focused services, orchestrated by a central workflow manager. This structure supports incremental, high-performance analysis typical of automated documentation tooling.

### Major Modules
- **Pipeline Orchestrator** (`analysis_pipeline.rb`): Serves as the primary entry point, managing the analysis workflow, task sequencing, and result aggregation.
- **Specialized Parsers & Scanners** (`source_parser.rb`, `yard_reader.rb`, `schema_parser.rb`, `model_association_parser.rb`, `import_extractor.rb`, `generic_scanner.rb`): Extract specific structural and semantic metadata. They handle raw source code, inline YARD documentation, schema definitions, model relationships, and dependency imports. `generic_scanner.rb` likely provides baseline or universal scanning capabilities.
- **Analysis Services** (`diff_service.rb`, `orphans_service.rb`): Perform higher-level intelligence on parsed data. The diff service identifies changes between code states, while the orphans service detects unused, disconnected, or stale documentation/code elements.
- **Caching Layer** (`analysis_cache.rb`): Persists intermediate and final analysis results to optimize performance and enable efficient re-analysis.

### Module Relationships & Data Flow
1. **Orchestration**: `analysis_pipeline.rb` initializes the analysis context and coordinates the execution order of parsers and services.
2. **Ingestion & Enrichment**: Parsed data from the scanner/parser modules flows into a shared analysis context. Dependencies, model links, and documentation are normalized and linked across sources.
3. **Advanced Processing**: The enriched context is passed to `diff_service.rb` and `orphans_service.rb`, which run comparative and completeness checks to surface documentation gaps or structural drift.
4. **Caching Integration**: Throughout the pipeline, `analysis_cache.rb` is queried before expensive operations and updated upon completion. This enables skip-on-cache for unchanged files and supports incremental analysis runs.

### Key Architectural Traits
- **Single-Responsibility Design**: Each file addresses one analytical concern, enhancing maintainability and testability.
- **Sequential Pipeline Pattern**: Data progresses linearly from raw parsing → semantic enrichment → differential/orphan detection → caching, aligning with industry-standard static analysis flows.
- **Namespace Cohesion**: All components operate within the `AutoDoc` module, reinforcing a unified purpose centered on automated documentation generation and codebase introspection.

## Dependencies Overview


| Name | Type |
|------|------|

| ripper | stdlib/gem |

| set | stdlib/gem |

| yard | stdlib/gem |



## Related Documents

- [INDEX.md](INDEX.md) — Detailed file and symbol index
- [AGENTS.md](AGENTS.md) — Agent-oriented module documentation


---

*Generated by auto-doc. Review and commit these drafts as your documentation.*
