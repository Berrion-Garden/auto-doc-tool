


# auto-doc-tool

> Generated by auto-doc on 2026-07-16 02:53:58 PDT

## System Overview

Auto-generated architecture documentation for auto-doc-tool.

## Architecture Style

The system follows a **modular pipeline architecture** with clear separation of concerns. The overall design is **plugin‑like**, where each major responsibility (analysis, transformation, generation, LLM interaction, reporting) is encapsulated in separate submodules under the `AutoDoc` namespace. The orchestrator acts as a central coordinator, executing a sequence of steps (pipeline) that process source code, build intermediate data structures, and finally produce documentation artifacts. This pipeline can be extended by adding new steps or generators. The architecture also incorporates a **layered data flow**: raw source text is first parsed into structured metadata (AST, YARD objects, database schemas), then transformed into higher‑level representations (class hierarchies, container data flows, ER relationships), and finally rendered into output files. The LLM integration is treated as a pair of services (client, prompt builder, response parser, summarizer) that can be invoked during generation steps to enhance content with natural language descriptions. The project is built with testability in mind, providing mock helpers for the LLM and extensive RSpec specs.

## Module Map

| Module | Responsibility |
|--------|---------------|

| **SourceParser** | parses Ruby source files into an AST or structure. |

| **YardReader** | reads YARD documentation comments from classes, modules, methods. |

| **SchemaParser** | interprets database schemas (e.g., from Rails migrations) to capture table columns and types. |

| **ModelAssociationParser** | extracts ActiveRecord associations (has_many, belongs_to, etc.). |

| **ImportExtractor** | resolves require/import dependencies between files. |

| **DiffService** | compares two snapshots of a project to detect changes for incremental documentation. |

| **OrphansService** | finds files or classes that lack documentation. |

| **AnalysisCache** | caches parsed results to speed up repeated runs. |

| **Pipeline** | manages a sequence of steps (e.g., manifest_step, architecture_step, diagram_step, readme_step, and index_summary_vectors_step). Each step is a separate class (e.g., `AgentsMdStep`, `ArchitectureStep`, `DiagramStep`, `ManifestStep`). |

| **BaseStep** | provides common setup for steps (project directory, output configuration). |

| **MetricsHelper** | collects timing and performance metrics during the pipeline run. |

| **ArchitectureGenerator** | generates a high‑level architecture description document. |

| **ReadmeGenerator** | creates or updates a README.md for the target project. |

| **IndexGenerator** | builds an index of all documented items. |

| **SummaryGenerator** | produces a summary of changes or coverage. |

| **AgentsMdGenerator** | generates a markdown file listing AI agents or helpers (likely for LLM context). |

| **ClassDiagramGenerator**, **C4DiagramGenerator**, **ErdGenerator**, **DiagramGenerator** | produce graph‑based diagrams (may use Mermaid or Graphviz). |

| **MapGenerator** | creates a site map or file tree. |

| **VectorGenerator** | creates vector embeddings (e.g., for semantic search). |

| **TemplateHelper** | assists with rendering templates (ERB or custom). |

| **Client** | API client (likely HTTP‑based) to send prompts and receive responses. |

| **PromptBuilder** | constructs effective prompts from code metadata. |

| **ResponseParser** | parses the LLM’s output into structured documentation content. |

| **Summarizer** | condenses or generates human‑readable summaries for classes, methods, etc. |

| **FilesDataBuilder** | builds a representation of the project file tree. |

| **ClassHierarchyBuilder** | computes inheritance and mixin relationships. |

| **ContainerDataFlowBuilder** | identifies data flows between architectural containers (e.g., controller → model → database). |

| **ErdRelationshipBuilder** | constructs entity‑relationship links from model associations. |

| **GraphDataBuilder** | creates a generic graph data structure for diagram generators. |

| **Config** | loads YAML configuration files and merges with defaults. |

| **Cli** | command‑line interface (Thor or similar) for running the tool. |

| **Server** | a small web server (probably Sinatra or Rack) to serve generated documentation. |

| **DocumentationIndex** | a persistent index of all documented items, supporting search. |

| **SearchService** | full‑text or semantic search over the documentation index. |

| **AgentQueryService** | likely an alternative query interface for AI agents to retrieve documentation. |

| **Reporter** (`AuditReporter`, `CompletenessChecker`) | produces coverage reports and audits documentation gaps. |

| **Utils** | `FileTreeBuilder`, `MarkdownHelper`, `OutputFormatter`, `TimestampTracker`, `YamlConfigLoader`. |


## Data Flow


_No data flows defined._


## Design Decisions


_No design decisions recorded._


## Diagrams




- [C4 Context](diagrams/c4_context.mmd)



- [C4 Container](diagrams/c4_container.mmd)



- [Class Diagram](diagrams/class_diagram.mmd)




---

*Generated by auto-doc. Review and commit as architecture documentation.*
