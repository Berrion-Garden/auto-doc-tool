
# SUMMARY: auto-doc-tool

> **Generated by auto-doc v1.0.0** on 2026-07-16 02:53:41 PDT

## Purpose

The **auto-doc-tool** module is a Ruby-based automated documentation generator. Its primary purpose is to analyze a Ruby project's source code and produce comprehensive, multi-format documentation with minimal manual effort. The tool leverages static analysis (via YARD, schema parsing, etc.) and optionally integrates with a large language model (LLM) to generate human-readable summaries and descriptions.

### Key Components

- **CLI & Configuration** – Entry point for running the tool (`cli.rb`, `config.rb`), accepting project paths and options.
- **Analyzer** – Contains modules for parsing source code (`source_parser.rb`), extracting YARD comments (`yard_reader.rb`), parsing database schemas (`schema_parser.rb`), detecting orphaned documentation (`orphans_service.rb`), analyzing diffs (`diff_service.rb`), and extracting imports/code dependencies (`import_extractor.rb`, `model_association_parser.rb`). Also includes an analysis pipeline (`analysis_pipeline.rb`) and a result cache (`analysis_cache.rb`).
- **Generator** – Generates output artifacts: architecture diagrams (`architecture_generator.rb`), C4 diagrams (`c4_diagram_generator.rb`), class diagrams (`class_diagram_generator.rb`), entity-relationship diagrams (`erd_generator.rb`), dependency maps (`map_generator.rb`), vector embeddings (`vector_generator.rb`), an index (`index_generator.rb`), a README (`readme_generator.rb`), a summary (`summary_generator.rb`), and an "agents" markdown file (`agents_md_generator.rb`). All generators share a `template_helper.rb` and a base `diagram_generator.rb`.
- **LLM Integration** – Provides an abstraction for interacting with LLMs: client (`client.rb`), prompt builder (`prompt_builder.rb`), response parser (`response_parser.rb`), and summarizer (`summarizer.rb`). The `llm.rb` module ties these together.
- **Orchestrator** – Manages the overall documentation generation process as a pipeline (`pipeline.rb`) with discrete steps (`base_step.rb`, `diagram_step.rb`, `architecture_step.rb`, `agents_md_step.rb`, `readme_step.rb`, `manifest_step.rb`, `index_summary_vectors_step.rb`). Includes metrics tracking (`metrics_helper.rb`).
- **Transformer** – Builds data structures for generating diagrams and reports: class hierarchy (`class_hierarchy_builder.rb`), container data flow (`container_data_flow_builder.rb`), ERD relationships (`erd_relationship_builder.rb`), file listings (`files_data_builder.rb`), and graph data (`graph_data_builder.rb`). The top-level `transformer.rb` orchestrates these builders.
- **Reporter** – Audits documentation completeness (`completeness_checker.rb`, `audit_reporter.rb`) and provides a search service (`search_service.rb`) and a built-in server (`server.rb`) for browsing generated docs.
- **Utilities** – Helper modules for file tree representation (`file_tree_builder.rb`), markdown formatting (`markdown_helper.rb`), output formatting (`output_formatter.rb`), timestamp tracking (`timestamp_tracker.rb`), and YAML configuration loading (`yaml_config_loader.rb`).
- **Testing & Fixtures** – Includes extensive test suites (`spec/`) and sample/fixture Ruby projects to validate the tool's behavior. Notable test helpers include LLM mocking (`llm_mock_helper.rb`) and an end-to-end runner (`e2e_runner.rb`).

In summary, **auto-doc-tool** is a modular, pipeline-driven documentation engine that automates analysis, summarization, and generation of static documentation files for Ruby projects, supporting both traditional static analysis and AI-assisted description.

## Key Components


| Component | Type | Summary |
|-----------|------|---------|

| auto-doc-tool | module | The project is an **automatic documentation generation tool** (auto-doc-tool) that analyzes Ruby source code and produces various documentation artifacts (markdown files, diagrams, summaries). It includes a set of fixtures (a Rails project, a sample Ruby project) used for testing.

## Component Relationships & Dependencies

- **Core Module `AutoDoc`** – Entry point; loads all submodules.
- **Orchestrator** – Drives the pipeline: `Pipeline` coordinates steps (`ArchitectureStep`, `DiagramStep`, `ReadmeStep`, `ManifestStep`, etc.). Each step uses specific generators, transformers, or analyzers.
- **Analyzer** (`Analyzer::*`) – Parses source code via `SourceParser`, `YardReader`, `ImportExtractor`, `ModelAssociationParser`, `SchemaParser`. Produces structured analysis data. `AnalysisCache` stores intermediate results.
- **Transformer** (`Transformer::*`) – Converts analysis data into forms ready for generation: `ClassHierarchyBuilder`, `ContainerDataFlowBuilder`, `ErdRelationshipBuilder`, `GraphDataBuilder`, `FilesDataBuilder`.
- **Generator** (`Generator::*`) – Produces final output files: `ArchitectureGenerator`, `ClassDiagramGenerator`, `ErdGenerator`, `C4DiagramGenerator`, `MapGenerator`, `IndexGenerator`, `ReadmeGenerator`, `SummaryGenerator`, `VectorGenerator`. Uses `TemplateHelper` for markdown rendering.
- **LLM** (`LLM::*`) – Integrates with language models: `Client` (API calls), `PromptBuilder`, `ResponseParser`, `Summarizer`. Used by `Summarizer` (likely called by orchestration steps).
- **Reporter** (`Reporter::*`) – Audits documentation: `CompletenessChecker`, `AuditReporter`.
- **Utils** (`Utils::*`) – Helpers: `FileTreeBuilder`, `MarkdownHelper`, `OutputFormatter`, `TimestampTracker`, `YamlConfigLoader`.
- **Server** – Serves generated documentation via a simple web interface.
- **CLI** – Command-line interface to trigger the pipeline.
- **Config** – Loads YAML configuration.

## Data Flow

1. **Input**: Ruby project source code (fixtures or user project) plus configuration.
2. **Analysis Phase**: The `Analyzer` reads files, extracts classes/modules, relationships, associations, database schema (from migrations), and inline documentation (YARD). Results stored in a `DocumentationIndex`.
3. **Transformation Phase**: `Transformer` consumes the index to build hierarchical class structures, container data flows, ER diagrams, and graph data.
4. **Generation Phase**: `Generator` uses transformed data to write:
   - Markdown files (README, summary, index, architecture).
   - Diagram files (C4, class, ERD, map).
   - Vector embeddings (for search).
5. **LLM Summarization** (optional): `Summarizer` takes source code snippets or parsed data and generates human-readable summaries via an LLM client.
6. **Orchestration**: The `Pipeline` runs steps sequentially; each step may read from the shared `DocumentationIndex` and output to the filesystem.
7. **Output**: A set of documentation files (`.md`, `.png`, `.json`) in a specified output directory.
8. **Testing**: Fixture projects and specs (`spec/auto_doc/*`) validate each component independently, often mocking the analyzers/generators.

## Key Dependencies

- `Orchestrator` → `Analyzer`, `Transformer`, `Generator`, `LLM`, `Reporter`
- `Analyzer` → `YardReader`, `SourceParser`, `ImportExtractor`, etc.
- `Transformer` → `DocumentationIndex`
- `Generator` → `TemplateHelper`, `Transformer` output
- `LLM` → external API (e.g., OpenAI)
- `Server` → generated index files
- `CLI` → `Orchestrator`, `Config` |



## Architecture Pattern

The **auto-doc-tool** is an automated documentation generation system for Ruby projects (including Rails). It analyzes source code, extracts structural and relational information, optionally uses an LLM to generate summaries, and produces multiple documentation artifacts (markdown, diagrams, architecture views).

## Overall Architecture

The tool follows a **pipeline orchestration** pattern. A central **Orchestrator** manages a series of sequential steps (e.g., analysis, diagram generation, README creation). Each step delegates to specialized **Generators** and **Transformers** that produce specific outputs. The system is driven by a **CLI** and can also run as a **server** or expose a **search service** over the generated documentation.

## Major Modules and Their Relationships

- **`AutoDoc::CLI`** – Entry point for command-line usage; parses arguments, loads configuration (`Config`), and starts the pipeline.

- **`AutoDoc::Config`** – Central configuration loader for project-specific settings (e.g., LLM provider, output paths, exclusions).

- **`AutoDoc::Orchestrator`** – Core pipeline manager:
  - Contains sequential steps (e.g., `ManifestStep`, `ArchitectureStep`, `DiagramStep`, `ReadmeStep`, `IndexSummaryVectorsStep`, `AgentsMdStep`).
  - Each step reads precomputed data from the **Analyzer** and calls appropriate **Generators**.
  - Uses `MetricsHelper` for timing and logging.

- **`AutoDoc::Analyzer`** – Parses the target project’s source code:
  - **`SourceParser`** – Extracts classes, modules, methods.
  - **`YardReader`** – Reads existing YARD documentation.
  - **`SchemaParser`** – Parses database schemas (Rails migrations).
  - **`ModelAssociationParser`** – Understands model relationships.
  - **`ImportExtractor`** – Identifies dependencies between files.
  - **`OrphansService`** – Finds undocumented code elements.
  - **`DiffService`** – Compares current vs. previous analysis for incremental updates.
  - **`AnalysisCache`** – Caches parsed results to improve performance.
  - **`AnalysisPipeline`** – Orchestrates the full analysis workflow.

- **`AutoDoc::Transformer`** – Converts raw analysis data into structured representations used by generators:
  - **`ClassHierarchyBuilder`** – Class inheritance / module inclusion trees.
  - **`ContainerDataFlowBuilder`** – Data flows between containers (e.g., controllers, models).
  - **`ErdRelationshipBuilder`** – Entity-Relationship diagrams from model associations.
  - **`FilesDataBuilder`** – File-level metadata.
  - **`GraphDataBuilder`** – General-purpose graph structures.

- **`AutoDoc::Generator`** – Produces the final documentation artifacts:
  - **`ReadmeGenerator`** – Creates or updates a project README.
  - **`ArchitectureGenerator`** – Architectural overview (e.g., layers, components).
  - **`DiagramGenerator`** (and sub‑generators like `ClassDiagramGenerator`, `C4DiagramGenerator`, `ERDGenerator`) – Renders various diagrams.
  - **`AgentsMdGenerator`** – Generates a machine‑readable agent‑specific markdown file.
  - **`IndexGenerator`**, **`SummaryGenerator`**, **`VectorGenerator`**, **`MapGenerator`** – Produce indexes, summaries, and vector embeddings for search.
  - **`TemplateHelper`** – Provides shared rendering utilities (e.g., ERB templates).

- **`AutoDoc::LLM`** – Interfaces with a large language model:
  - **`Client`** – Handles HTTP API calls to the LLM provider.
  - **`PromptBuilder`** – Constructs prompts from analysis context.
  - **`ResponseParser`** – Extracts structured data from LLM responses.
  - **`Summarizer`** – Generates concise summaries for code elements.

- **`AutoDoc::Reporter`** – Quality assurance:
  - **`AuditReporter`** – Produces an audit report of missing/incorrect documentation.
  - **`CompletenessChecker`** – Evaluates documentation coverage.

- **`AutoDoc::Utils`** – Shared helpers:
  - **`FileTreeBuilder`** – Builds directory/file trees.
  - **`MarkdownHelper`** – Markdown formatting utilities.
  - **`OutputFormatter`** – Formats text/terminal output.
  - **`TimestampTracker`** – Tracks generation times.
  - **`YamlConfigLoader`** – Reads YAML config files.

- **`AutoDoc::Server`** – A lightweight web server to browse generated documentation.

- **`AutoDoc::SearchService`** – Provides search over indexed documentation.

- **`AutoDoc::Version`**, **`AutoDoc.rb`** – Module root and version constant.

## Relationship to Fixtures

The project includes **fixtures** (sample Ruby/Rails projects) used for testing and integration checks. These demonstrate the tool’s ability to analyze real-world patterns (models, controllers, migrations, serializers, jobs, mailers, concerns, etc.) and validate that generated documentation matches expected outputs.

## Summary

`auto-doc-tool` is a modular, pipeline‑based documentation generator. It first **analyzes** a Ruby project, then **transforms** the data into structured graphs, and finally **generates** various documentation outputs (Markdown, diagrams, indexes). An optional LLM integration enriches summaries, and the orchestrator coordinates the entire workflow. The architecture supports extensibility: new analyzers, transformers, or generators can be added by following the same patterns.

## Dependencies Overview


| Name | Type |
|------|------|

| ../auto_doc/server | local |

| active_record | stdlib/gem |

| auto_doc | stdlib/gem |

| auto_doc/agent_query_service | path |

| auto_doc/analyzer/analysis_cache | path |

| auto_doc/analyzer/analysis_pipeline | path |

| auto_doc/analyzer/diff_service | path |

| auto_doc/analyzer/import_extractor | path |

| auto_doc/analyzer/model_association_parser | path |

| auto_doc/analyzer/orphans_service | path |

| auto_doc/analyzer/schema_parser | path |

| auto_doc/analyzer/source_parser | path |

| auto_doc/analyzer/yard_reader | path |

| auto_doc/cli | path |

| auto_doc/config | path |

| auto_doc/documentation_index | path |

| auto_doc/generator/agents_md_generator | path |

| auto_doc/generator/architecture_generator | path |

| auto_doc/generator/c4_diagram_generator | path |

| auto_doc/generator/class_diagram_generator | path |

| auto_doc/generator/diagram_generator | path |

| auto_doc/generator/erd_generator | path |

| auto_doc/generator/index_generator | path |

| auto_doc/generator/map_generator | path |

| auto_doc/generator/readme_generator | path |

| auto_doc/generator/summary_generator | path |

| auto_doc/generator/template_helper | path |

| auto_doc/generator/vector_generator | path |

| auto_doc/llm | path |

| auto_doc/orchestrator | path |

| auto_doc/orchestrator/agents_md_step | path |

| auto_doc/orchestrator/architecture_step | path |

| auto_doc/orchestrator/base_step | path |

| auto_doc/orchestrator/diagram_step | path |

| auto_doc/orchestrator/index_summary_vectors_step | path |

| auto_doc/orchestrator/manifest_step | path |

| auto_doc/orchestrator/metrics_helper | path |

| auto_doc/orchestrator/pipeline | path |

| auto_doc/orchestrator/readme_step | path |

| auto_doc/reporter/audit_reporter | path |

| auto_doc/reporter/completeness_checker | path |

| auto_doc/search_service | path |

| auto_doc/server | path |

| auto_doc/tester/e2e_runner | path |

| auto_doc/transformer | path |

| auto_doc/utils/file_tree_builder | path |

| auto_doc/utils/markdown_helper | path |

| auto_doc/utils/output_formatter | path |

| auto_doc/utils/timestamp_tracker | path |

| auto_doc/utils/yaml_config_loader | path |

| auto_doc/version | path |

| AutoDoc::Generator::TemplateHelper | stdlib/gem |

| AutoDoc::Orchestrator::MetricsHelper | stdlib/gem |

| Enumerable | stdlib/gem |

| erb | stdlib/gem |

| fileutils | stdlib/gem |

| json | stdlib/gem |

| llm/client | path |

| llm/prompt_builder | path |

| llm/response_parser | path |

| llm/summarizer | path |

| MetricsHelper | stdlib/gem |

| net/http | path |

| pathname | stdlib/gem |

| rack/test | path |

| Rack::Test::Methods | stdlib/gem |

| rails | stdlib/gem |

| ripper | stdlib/gem |

| set | stdlib/gem |

| shellwords | stdlib/gem |

| sinatra/base | path |

| spec_helper | stdlib/gem |

| support/llm_mock_helper | path |

| TemplateHelper | stdlib/gem |

| thor | stdlib/gem |

| time | stdlib/gem |

| timeout | stdlib/gem |

| tmpdir | stdlib/gem |

| transformer | stdlib/gem |

| transformer/class_hierarchy_builder | path |

| transformer/container_data_flow_builder | path |

| transformer/erd_relationship_builder | path |

| transformer/files_data_builder | path |

| transformer/graph_data_builder | path |

| uri | stdlib/gem |

| utils/markdown_helper | path |

| utils/yaml_config_loader | path |

| yaml | stdlib/gem |

| yard | stdlib/gem |



## Related Documents

- [INDEX.md](INDEX.md) — Detailed file and symbol index
- [AGENTS.md](AGENTS.md) — Agent-oriented module documentation


---

*Generated by auto-doc. Review and commit these drafts as your documentation.*
