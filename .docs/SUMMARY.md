
# SUMMARY: auto-doc-tool

> **Generated by auto-doc v1.0.0** on 2026-07-16 11:34:54 PDT

## Purpose

Based on the provided file structure and metadata, here is a concise summary of the `auto-doc-tool` module:

### **Purpose**
`auto-doc-tool` is an automated documentation generation system designed to scan, interpret, and produce comprehensive, up-to-date documentation for software projects. It primarily targets Ruby/Rails applications but supports cross-language analysis (Python, JavaScript, etc.). Its goal is to eliminate manual documentation overhead by automatically extracting architectural relationships, code logic, and existing inline comments, then synthesizing them into structured, human-readable, and AI-ready formats.

### **Core Functionality**
- **Static & Schema Analysis:** Parses source files, YARD-style comments, database migrations, and schema definitions to build a complete project knowledge graph.
- **Incremental Processing:** Uses caching and diff services to analyze only changed or new files, improving performance on large codebases.
- **LLM Integration:** Leverages large language models to summarize code, generate architectural descriptions, fill documentation gaps, and create vector embeddings for semantic search.
- **Multi-Format Output Generation:** Produces Markdown documentation, technical diagrams, indexes, and vectorized content tailored for developers and AI agents.

### **Key Components**
- **Analyzer:** Extracts structural metadata by scanning source files, reading YARD comments, parsing DB schemas, and resolving model associations and import dependencies.
- **Transformer:** Converts raw parsed data into structured relationship graphs, including class hierarchies, ERD relationships, container data flows, and file trees.
- **LLM Layer:** Handles AI interactions through prompt building, client communication, response parsing, and text summarization to enhance or generate documentation content.
- **Orchestrator & Pipeline:** Coordinates a multi-step generation workflow. Manages execution steps (architecture overview, diagram generation, README creation, agent markdown, vector indexing) and tracks progress/metrics.
- **Generators:** Specialized modules that render outputs in specific formats, including C4 diagrams, class diagrams, ERDs, architecture maps, READMEs, and search-ready index/vector files.
- **Reporter & Auditor:** Evaluates documentation health by checking coverage completeness, identifying undocumented or orphaned classes/modules, and generating audit reports.
- **CLI & Server:** Provides a command-line interface for local execution and a lightweight HTTP server for remote interaction, configuration management, and result retrieval.

### **Integration & Ecosystem**
The tool is designed to slot into CI/CD pipelines or development workflows, automatically keeping documentation synchronized with code changes. It outputs markdown files, diagram images, and vector embeddings, making it suitable for developer portals, AI-assisted code navigation, and long-term project onboarding.

## Key Components


| Component | Type | Summary |
|-----------|------|---------|

| auto-doc-tool | module | Based on the provided metadata, here is a concise architectural summary of the `auto-doc-tool` project:

### 🔷 Architecture Overview
The project follows a **pipeline-driven architecture** built in Ruby with Node.js runtime dependencies for server, AI, and schema handling. It is designed to automatically analyze codebases, extract structural relationships, enrich content via AI, and generate multi-format documentation (Markdown, diagrams, ERDs, search vectors).

### 🔗 Component Relationships & Dependencies
Components are organized into logical layers under `lib/auto_doc/`, with clear separation of concerns:

| Layer | Key Components | Responsibility |
|-------|----------------|----------------|
| **Entry & Orchestration** | `cli.rb`, `server.rb`, `orchestrator/` | Expose the tool via CLI/API. `Pipeline` and `base_step` coordinate sequential execution of analysis, generation, and reporting phases. |
| **Analysis** | `analyzer/source_parser.rb`, `schema_parser.rb`, `import_extractor.rb`, `model_association_parser.rb`, `yard_reader.rb` | Scan and parse Ruby/Rails/Python source files, extract class definitions, database schemas, and dependency graphs. `analysis_cache.rb` and `diff_service.rb` track changes to avoid redundant work. |
| **Transformation** | `transformer/class_hierarchy_builder.rb`, `container_data_flow_builder.rb`, `graph_data_builder.rb`, `erd_relationship_builder.rb` | Convert raw parse output into structured, traversable data models: inheritance trees, data flows, graph nodes/edges, and relational mapping. |
| **AI/LLM** | `llm/client.rb`, `prompt_builder.rb`, `response_parser.rb`, `summarizer.rb` | Interface with external AI services. Handle prompt templating, stream parsing, and content summarization to enrich documentation or resolve ambiguous code structures. |
| **Generation** | `generator/` (markdown, diagram, c4, class, erd, vector, index, readme) | Render transformed + AI-enriched data into final artifacts. Produces architecture diagrams, markdown reports, vector embeddings, and searchable indexes. |
| **Reporting & Query** | `reporter/audit_reporter.rb`, `completeness_checker.rb`, `search_service.rb`, `documentation_index.rb` | Track documentation coverage, validate completeness against baselines, and manage persistent storage/retrieval of generated docs. |
| **Utilities** | `utils/`, `config.rb`, `errors.rb` | Cross-cutting concerns: YAML config loading, file tree construction, markdown formatting, timestamp tracking, and standardized error handling. |

### 🔄 Data Flow Pipeline
1. **Ingestion & Configuration**: `CLI` or `Server` receives a target project path and loads `config.yaml`.
2. **Static Analysis**: `Analyzer` modules recursively read files, extract imports, parse ORM models/databases, and cache results. `DiffService` filters only changed/added code.
3. **Structural Transformation**: `Transformer` builds normalized graphs (class hierarchies, data flows, ERD relationships) from the parsed AST/source data.
4. **AI Enrichment (Optional)**: `Orchestrator` passes critical code snippets to the `LLM` layer for summarization, architectural inference, or agent prompt generation. Responses are parsed and merged back into the data model.
5. **Artifact Generation**: `Generator` modules consume the enriched graph to produce Markdown documentation, C4/Class/ERD diagrams, search vectors, and index overviews.
6. **Storage & Query**: Generated content and vectors are persisted via `DocumentationIndex`. `SearchService` enables full-text/vector queries. `Server` exposes REST/HTTP endpoints for real-time access.
7. **Audit & Reporting**: `Reporter` and `CompletenessChecker` evaluate generation results against coverage thresholds, outputting audit trails and metrics.

### 📦 External Dependencies (Node.js Ecosystem)
The `node_modules/` directory indicates runtime dependencies used primarily for the web server, AI integration, and schema validation:
- **`@modelcontextprotocol/sdk`**: Enables standardized AI agent communication and structured data exchange.
- **`@hono/node-server` / `express`**: Powers the HTTP routing and API layer.
- **`zod` & `ajv`**: Provide runtime schema validation for configuration, LLM payloads, and API contracts.
- **`better-sqlite3`**: Handles local, synchronous storage for the documentation index and analysis cache.
- **`@mixmark-io/domino` / `turndown`**: Parse and convert HTML/Markdown content during document rendering.

### 📝 Notes on Non-Core Paths
- `fixtures/`: Sample Ruby, Rails, and Python projects used for integration testing and pipeline validation.
- `spec/`: RSpec test suite mirroring the `lib/` structure.
- `node_modules/`: Third-party libraries; not part of the internal component model.

This architecture emphasizes modularity, caching, and a clear separation between code parsing, structural transformation, AI enrichment, and documentation rendering. |



## Architecture Pattern

### Overall Architecture
`auto-doc-tool` follows a **modular, pipeline-driven architecture** designed to automatically scan source codebases and produce comprehensive, multi-format documentation. The system is organized around a clear, stage-gated data flow: **ingestion → orchestration → analysis → transformation → generation → reporting/serving**. It emphasizes separation of concerns, incremental execution via caching/diffing, and AI-assisted augmentation to bridge gaps in manual documentation.

### Major Modules & Responsibilities
- **CLI & Configuration (`cli.rb`, `config.rb`)**: Entry point that parses user commands, loads project settings, and initializes the runtime context.
- **Orchestrator (`orchestrator/`)**: Manages the end-to-end execution workflow using a composable, step-based pipeline. Coordinates task sequencing, dependency resolution, and fallback handling across analysis, generation, and reporting phases.
- **Analyzer (`analyzer/`)**: Extracts structural and semantic metadata from the target codebase. Handles source file parsing, YARD/inline comment extraction, database schema introspection, model association mapping, import/dependency tracking, and incremental diff/caching logic.
- **Transformer (`transformer/`)**: Converts raw analyzed metadata into structured intermediate representations. Builds class hierarchies, data flow graphs, ERD relationships, and filesystem trees optimized for downstream diagram generators and AI context builders.
- **Generator (`generator/`)**: Produces final documentation artifacts. Contains specialized modules for class diagrams, C4 architecture diagrams, ERDs, markdown summaries, READMEs, agent-specific documentation, and vector/graph indexes.
- **LLM Integration (`llm/`)**: Handles communication with Large Language Models. Manages prompt templating, client requests, response parsing, and AI-driven summarization/enrichment invoked by specific generation steps.
- **Reporter & Audit (`reporter/`)**: Evaluates documentation coverage, identifies undocumented elements, and generates completeness/audit reports to guide manual supplementation.
- **Server & Search (`server.rb`, `search_service.rb`)**: Serves the generated documentation via a local web interface and provides query capabilities over the central documentation index.
- **Utilities (`utils/`)**: Shared helpers for YAML configuration loading, markdown formatting, timestamp tracking, file tree construction, and output styling.

### Module Relationships & Data Flow
1. **Initialization**: The CLI loads configuration and hands control to the Orchestrator.
2. **Analysis Phase**: The Orchestrator triggers the Analyzer, which scans the codebase and populates a central **Documentation Index**. Caching and diff services ensure only changed or new elements are reprocessed.
3. **Transformation Phase**: Raw metadata flows into the Transformer, which constructs interconnected graph structures (class hierarchies, data flows, relationship maps) tailored for visualization and AI context.
4. **Generation Phase**: The Orchestrator routes structured data to specialized Generators. LLM components are called as auxiliary services to generate summaries, enrich descriptions, or fill coverage gaps.
5. **Reporting & Serving**: Once artifacts are produced, the Reporter validates coverage and generates audit metrics. The Server exposes the output, while the Search Service enables interactive navigation of the index.

### Key Architectural Patterns
- **Step-Based Pipeline**: Execution is decoupled into reusable, swappable steps, enabling easy extension or custom workflow composition.
- **Multi-Stage Data Processing**: Separates low-level parsing from high-level artifact generation, reducing complexity and improving maintainability.
- **Graph-Centric Intermediate Representation**: Transformed data is structured as interconnected graphs rather than flat records, enabling simultaneous generation of multiple diagram types from a single pass.
- **Incremental & Cache-Aware Design**: Diff and cache services minimize processing overhead for repeated runs by tracking metadata changes.
- **Pluggable AI Augmentation**: LLM integration is treated as an optional enrichment layer, invoked selectively without polluting core parsing or generation logic.
- **Centralized Indexing**: All discovered elements and generated outputs are registered in a unified index, supporting fast lookups, search, and incremental syncs.

This architecture balances automation with extensibility, allowing developers to generate accurate, multi-format documentation while retaining clear hooks for customization, AI enhancement, and incremental updates.

## Dependencies Overview


| Name | Type |
|------|------|

| ../auto_doc/server | local |

| active_record | stdlib/gem |

| applets abd APPLET elements in a document. | stdlib/gem |

| auto_doc | stdlib/gem |

| auto_doc/agent_query_service | path |

| auto_doc/analyzer/analysis_cache | path |

| auto_doc/analyzer/analysis_pipeline | path |

| auto_doc/analyzer/diff_service | path |

| auto_doc/analyzer/generic_scanner | path |

| auto_doc/analyzer/import_extractor | path |

| auto_doc/analyzer/model_association_parser | path |

| auto_doc/analyzer/orphans_service | path |

| auto_doc/analyzer/schema_parser | path |

| auto_doc/analyzer/source_parser | path |

| auto_doc/analyzer/yard_reader | path |

| auto_doc/cli | path |

| auto_doc/config | path |

| auto_doc/documentation_index | path |

| auto_doc/errors | path |

| auto_doc/generator/agents_md_generator | path |

| auto_doc/generator/agents_overview_generator | path |

| auto_doc/generator/architecture_generator | path |

| auto_doc/generator/c4_diagram_generator | path |

| auto_doc/generator/class_diagram_generator | path |

| auto_doc/generator/diagram_generator | path |

| auto_doc/generator/erd_generator | path |

| auto_doc/generator/index_generator | path |

| auto_doc/generator/map_generator | path |

| auto_doc/generator/readme_generator | path |

| auto_doc/generator/summary_generator | path |

| auto_doc/generator/template_helper | path |

| auto_doc/generator/vector_generator | path |

| auto_doc/llm | path |

| auto_doc/orchestrator | path |

| auto_doc/orchestrator/agents_md_step | path |

| auto_doc/orchestrator/agents_overview_step | path |

| auto_doc/orchestrator/architecture_step | path |

| auto_doc/orchestrator/base_step | path |

| auto_doc/orchestrator/diagram_step | path |

| auto_doc/orchestrator/index_summary_vectors_step | path |

| auto_doc/orchestrator/manifest_step | path |

| auto_doc/orchestrator/metrics_helper | path |

| auto_doc/orchestrator/pipeline | path |

| auto_doc/orchestrator/readme_step | path |

| auto_doc/reporter/audit_reporter | path |

| auto_doc/reporter/completeness_checker | path |

| auto_doc/search_service | path |

| auto_doc/server | path |

| auto_doc/tester/e2e_runner | path |

| auto_doc/transformer | path |

| auto_doc/utils/file_tree_builder | path |

| auto_doc/utils/markdown_helper | path |

| auto_doc/utils/output_formatter | path |

| auto_doc/utils/timestamp_tracker | path |

| auto_doc/utils/yaml_config_loader | path |

| auto_doc/version | path |

| AutoDoc::Generator::TemplateHelper | stdlib/gem |

| AutoDoc::Orchestrator::MetricsHelper | stdlib/gem |

| Enumerable | stdlib/gem |

| erb | stdlib/gem |

| fileutils | stdlib/gem |

| json | stdlib/gem |

| llm/client | path |

| llm/prompt_builder | path |

| llm/response_parser | path |

| llm/summarizer | path |

| MetricsHelper | stdlib/gem |

| net/http | path |

| pathname | stdlib/gem |

| rack/test | path |

| Rack::Test::Methods | stdlib/gem |

| rails | stdlib/gem |

| ripper | stdlib/gem |

| set | stdlib/gem |

| shellwords | stdlib/gem |

| sinatra/base | path |

| spec_helper | stdlib/gem |

| support/llm_mock_helper | path |

| tempfile | stdlib/gem |

| TemplateHelper | stdlib/gem |

| thor | stdlib/gem |

| time | stdlib/gem |

| timeout | stdlib/gem |

| tmpdir | stdlib/gem |

| transformer | stdlib/gem |

| transformer/class_hierarchy_builder | path |

| transformer/container_data_flow_builder | path |

| transformer/erd_relationship_builder | path |

| transformer/files_data_builder | path |

| transformer/graph_data_builder | path |

| uri | stdlib/gem |

| utils/markdown_helper | path |

| utils/yaml_config_loader | path |

| yaml | stdlib/gem |

| yard | stdlib/gem |



## Related Documents

- [INDEX.md](INDEX.md) — Detailed file and symbol index
- [AGENTS.md](AGENTS.md) — Agent-oriented module documentation


---

*Generated by auto-doc. Review and commit these drafts as your documentation.*
