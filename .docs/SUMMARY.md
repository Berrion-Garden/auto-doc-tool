
# SUMMARY: auto-doc-tool

> **Generated by auto-doc v1.0.0** on 2026-07-16 13:12:09 PDT

## Purpose

## Module Summary: `auto-doc-tool`

### Purpose
The `auto-doc-tool` module is an automated documentation generation and architectural analysis system designed to ingest codebases, extract structural and semantic information, and produce comprehensive, multi-format documentation artifacts. Its primary goal is to eliminate manual documentation overhead by combining static code analysis, diagram generation, and AI-driven enrichment, while maintaining documentation alignment with evolving code.

### What It Does
- Scans source files, database schemas, and dependency graphs to build a unified representation of a project's architecture.
- Extracts and interprets existing annotations (e.g., YARD comments) alongside inferred relationships (associations, imports, class hierarchies).
- Generates documentation outputs including Markdown guides, architecture maps, C4/class/ERD diagrams, AI-agent prompts, and coverage reports.
- Supports incremental analysis by tracking diffs and maintaining a searchable documentation index for efficient updates.
- Provides both a CLI for batch/CI usage and a server interface for interactive or integrated workflows.

### Key Components
- **Analyzer Pipeline**: Parses source code across multiple languages, extracts imports, model associations, schema structures, and existing documentation. Includes a generic scanner, YARD reader, and diff service for change detection.
- **LLM Integration Layer**: Interfaces with large language models to summarize code blocks, enrich documentation with context, generate AI-agent prompts, and parse structured model responses.
- **Documentation Generators**: Specialized modules that transform analyzed data into targeted outputs, including architecture maps, C4/class/ERD diagrams, READMEs, index pages, vector summaries, and AI agent documentation.
- **Orchestrator & Workflow Engine**: Coordinates the multi-step generation pipeline, managing data flow from initial scanning through transformation to final output assembly.
- **Reporting & Completeness Checker**: Audits the codebase to identify undocumented classes, orphaned files, and documentation gaps, producing structured audit reports.
- **Transformer Layer**: Converts raw parsed data into structured formats required for diagramming, graph analysis, and relationship mapping (e.g., class hierarchies, container data flows, ERD relationships).
- **CLI & Server Interface**: Exposes command-line execution for automated runs and a web server/API for interactive querying, integration, or continuous documentation monitoring.
- **Configuration & Indexing**: Manages YAML-based project settings, maintains a persistent documentation index, and handles timestamp tracking for cache management and incremental builds.

### Architecture Overview
The module follows a pipeline-driven architecture: `Scanning & Parsing` → `Data Transformation` → `LLM Enrichment` → `Orchestrated Generation` → `Reporting & Indexing`. Dependencies are managed through standard tooling, with the core library written in Ruby and supported by JavaScript/TypeScript packages for web server and API functionality. Test fixtures cover Ruby, Python, and JavaScript sample projects to validate multi-language compatibility and documentation coverage tracking.

## Key Components


| Component | Type | Summary |
|-----------|------|---------|

| auto-doc-tool | module | Based on the provided metadata, here is a concise architectural summary of the `auto-doc-tool` project:

### 🏗️ Architectural Overview
The project is a multi-language documentation automation tool built primarily in Ruby. It follows a modular, pipeline-driven architecture that ingests source code and schema files, analyzes them, enriches the data via AI/LLM services, and generates structured documentation artifacts. A Node.js runtime layer is integrated to power the HTTP server, search indexing, and AI context protocol handling.

### 🔗 Core Component Relationships
The system is organized into logical layers within `lib/auto_doc/`:

| Layer | Key Components | Responsibility |
|-------|----------------|----------------|
| **Orchestration** | `orchestrator/pipeline`, `base_step`, `*_step` | Coordinates the execution flow using a step-based pipeline pattern. Manages manifest creation, indexing, and summary generation. |
| **Analysis** | `analyzer/source_parser`, `model_association_parser`, `schema_parser`, `yard_reader`, `import_extractor` | Scans multi-language source files and database schemas. Extracts structural metadata, YARD comments, imports, and ORM associations. |
| **Transformation** | `transformer/graph_data_builder`, `class_hierarchy_builder`, `container_data_flow_builder`, `erd_relationship_builder` | Converts raw analysis data into structured internal representations (graphs, hierarchies, data flows, ERD relationships). |
| **AI/LLM** | `llm/client`, `prompt_builder`, `enricher`, `summarizer`, `response_parser` | Interfaces with external language models. Constructs context-aware prompts, manages streaming/polling responses, and processes structured outputs for enrichment. |
| **Generation** | `generator/*_generator` (C4, ERD, Class, Architecture, Markdown, Vectors, Index) | Renders transformed/enriched data into final documentation formats, diagrams, and search-index vectors. |
| **Services & UI** | `server`, `search_service`, `agent_query_service`, `documentation_index` | Hosts the generated documentation, provides semantic search, and handles agent-style query routing via Hono/Express. |
| **Utilities & Reporting** | `utils/`, `reporter/*`, `tester/e2e_runner` | File system operations, YAML config loading, markdown formatting, completeness auditing, and end-to-end validation. |

### 📦 Dependency Ecosystem
- **Internal**: Strictly organized under `lib/auto_doc/`. High cohesion between `analyzer` → `transformer` → `llm` → `generator`.
- **External (Node.js Runtime)**: The project embeds a Node.js ecosystem for modern web and AI operations:
  - `hono` & `@hono/node-server`: Lightweight, high-performance HTTP server and routing layer.
  - `@modelcontextprotocol/sdk`: Standardized AI context protocol for LLM tooling and agent communication.
  - `zod` & `ajv`: Runtime type validation and JSON schema enforcement.
  - `@mixmark-io/domino`: Server-side DOM/HTML parsing for content extraction or formatting.
- **Test Infrastructure**: `fixtures/` and `test_fixtures/` contain sample Rails, Ruby, Python, and partial documentation projects. `spec/` mirrors the `lib/` structure with RSpec tests, plus an `e2e` runner for integration validation.

### 🔄 Data Flow Pipeline
1. **Ingestion**: The CLI (`cli.rb`) reads project configuration (`yaml_config_loader.rb`) and discovers source files, dependencies, and DB schema files.
2. **Static Analysis**: `analyzer` components parse files, extract YARD/docs, resolve imports, and model associations. `diff_service` tracks incremental changes to avoid reprocessing unchanged files.
3. **Graph & Model Construction**: `transformer` components map extracted metadata into interconnected data structures (class trees, container boundaries, database relationships, data flow graphs).
4. **AI Enrichment**: `llm` components feed the structured graphs into prompts, call the LLM client, and parse responses to summarize, generate architectural context, or fill gaps in documentation.
5. **Artifact Generation**: `generator` components consume the enriched data to produce:
   - Visual diagrams (C4, Class, ERD)
   - Narrative documentation (Architecture, Agents, README)
   - Search-ready vectors and indices
6. **Delivery & Validation**: `reporter` checks documentation completeness. `server` exposes the generated content via HTTP, while `search_service` enables vector/keyword queries. `tester/e2e_runner` validates output fidelity.

### 📝 Key Architectural Notes
- **Pipeline-Driven Execution**: The orchestrator enforces a deterministic sequence, allowing steps like analysis, LLM enrichment, and generation to run independently or in parallel where possible.
- **Immutable Artifacts**: Generated documentation is treated as versioned output, with `analysis_cache` and `diff_service` optimizing subsequent runs.
- **Polyglot Agnostic Design**: While the tooling is Ruby-based, the analyzer and transformer are designed to handle Ruby, Python, JavaScript, and SQL schema metadata uniformly.
- **Server-First Delivery**: The integration of Hono and MCP SDK indicates the tool not only generates docs but also hosts them as a searchable, AI-agent-friendly service.

This architecture prioritizes modularity, incremental processing, and AI-augmented accuracy, delivering both human-readable and machine-queryable documentation from raw codebases. |



## Architecture Pattern

Based on the provided metadata, here is a concise architectural overview of the `auto-doc-tool` project:

### Overall Architecture
`auto-doc-tool` follows a **modular, pipeline-driven architecture** designed to automate software documentation generation. The system is structured around a sequential workflow: **Ingest → Analyze → Transform → Enrich → Generate → Validate**. It is primarily built in Ruby but leverages a lightweight Node.js-based server for CLI and web exposure, integrating modern AI capabilities to augment static code analysis.

### Major Modules & Responsibilities
| Module | Core Responsibility |
| :--- | :--- |
| **Analyzer** | Scans target projects to extract structural data from source code, database schemas, and existing comments (YARD). Handles dependency tracking, diff detection, and result caching for performance. |
| **Transformer** | Converts raw parsed output into structured internal models. Builds class hierarchies, data flow graphs, ERD relationships, and file trees to serve as input for generation. |
| **LLM Integration** | Manages AI-driven documentation enhancement. Contains clients, prompt builders, response parsers, enrichers, and summarizers to inject semantic context, architectural insights, and auto-generated descriptions. |
| **Generator** | Produces final documentation artifacts in multiple formats. Includes specialized modules for Markdown, C4 diagrams, class/ER diagrams, architecture overviews, search index vectors, and README files. |
| **Orchestrator** | Coordinates the end-to-end pipeline. Manages execution steps, tracks metrics/state, and sequences tasks (e.g., diagram generation, architecture docs, agent markdown) in a deterministic order. |
| **Reporter & Testing** | Validates output quality. Audits generated documentation for completeness and runs end-to-end test suites to ensure reliability across project types. |
| **CLI & Server** | Provides user-facing interfaces. The CLI handles batch/script execution, while the server exposes functionality via a web interface using adapter patterns for various runtime environments. |
| **Utilities & Config** | Supplies cross-cutting services: YAML configuration loading, markdown formatting, file tree construction, timestamp tracking, and centralized error handling. |

### Inter-Module Relationships & Data Flow
1. **Entry & Configuration:** The `CLI` or `Server` receives a target project path and optional configuration. The `Config` module loads and validates settings.
2. **Analysis Phase:** The `Analyzer` parses the project, extracts code/schema/YARD data, and populates a central `DocumentationIndex`. The `AnalysisCache` and `DiffService` optimize subsequent runs by skipping unchanged files.
3. **Transformation Phase:** The `Transformer` consumes the index to build abstract graphs and relationships (class hierarchies, data flows, ERDs).
4. **Orchestration & AI Enrichment:** The `Orchestrator/Pipeline` sequences generation steps. During specific phases, it delegates to `LLM` services to fetch AI-generated context, which is then parsed, enriched, and merged back into the pipeline state.
5. **Generation Phase:** `Generator` modules consume the transformed, AI-augmented data to produce formatted outputs (MD, diagrams, vector embeddings for search/RAG).
6. **Output & Validation:** The `Reporter` audits the generated docs for coverage gaps, while the `Tester` module ensures end-to-end correctness. Final artifacts are delivered via the CLI or served through the web interface.

### Key Design Characteristics
- **Pipeline-Centric:** Execution is broken into discrete, interchangeable steps, making the documentation workflow highly extensible.
- **Strict Separation of Concerns:** Parsing, modeling, AI interaction, and artifact production are decoupled to enable independent testing and updates.
- **AI-Augmented Analysis:** LLM services are deeply integrated to bridge the gap between static code scanning and semantic documentation.
- **Incremental & Efficient:** Caching, diff tracking, and vector indexing minimize redundant processing and enable fast regeneration.

## Dependencies Overview


| Name | Type |
|------|------|

| ../auto_doc/server | local |

| active_record | stdlib/gem |

| applets abd APPLET elements in a document. | stdlib/gem |

| auto_doc | stdlib/gem |

| auto_doc/agent_query_service | path |

| auto_doc/analyzer/analysis_cache | path |

| auto_doc/analyzer/analysis_pipeline | path |

| auto_doc/analyzer/diff_service | path |

| auto_doc/analyzer/generic_scanner | path |

| auto_doc/analyzer/import_extractor | path |

| auto_doc/analyzer/model_association_parser | path |

| auto_doc/analyzer/orphans_service | path |

| auto_doc/analyzer/schema_parser | path |

| auto_doc/analyzer/source_parser | path |

| auto_doc/analyzer/yard_reader | path |

| auto_doc/cli | path |

| auto_doc/config | path |

| auto_doc/documentation_index | path |

| auto_doc/errors | path |

| auto_doc/generator/agents_md_generator | path |

| auto_doc/generator/agents_overview_generator | path |

| auto_doc/generator/architecture_generator | path |

| auto_doc/generator/c4_diagram_generator | path |

| auto_doc/generator/class_diagram_generator | path |

| auto_doc/generator/diagram_generator | path |

| auto_doc/generator/erd_generator | path |

| auto_doc/generator/index_generator | path |

| auto_doc/generator/map_generator | path |

| auto_doc/generator/readme_generator | path |

| auto_doc/generator/summary_generator | path |

| auto_doc/generator/template_helper | path |

| auto_doc/generator/vector_generator | path |

| auto_doc/llm | path |

| auto_doc/orchestrator | path |

| auto_doc/orchestrator/agents_md_step | path |

| auto_doc/orchestrator/agents_overview_step | path |

| auto_doc/orchestrator/architecture_step | path |

| auto_doc/orchestrator/base_step | path |

| auto_doc/orchestrator/diagram_step | path |

| auto_doc/orchestrator/index_summary_vectors_step | path |

| auto_doc/orchestrator/manifest_step | path |

| auto_doc/orchestrator/metrics_helper | path |

| auto_doc/orchestrator/pipeline | path |

| auto_doc/orchestrator/readme_step | path |

| auto_doc/reporter/audit_reporter | path |

| auto_doc/reporter/completeness_checker | path |

| auto_doc/search_service | path |

| auto_doc/server | path |

| auto_doc/tester/e2e_runner | path |

| auto_doc/transformer | path |

| auto_doc/utils/file_tree_builder | path |

| auto_doc/utils/markdown_helper | path |

| auto_doc/utils/output_formatter | path |

| auto_doc/utils/timestamp_tracker | path |

| auto_doc/utils/yaml_config_loader | path |

| auto_doc/version | path |

| AutoDoc::Generator::TemplateHelper | stdlib/gem |

| AutoDoc::Orchestrator::MetricsHelper | stdlib/gem |

| Enumerable | stdlib/gem |

| erb | stdlib/gem |

| fileutils | stdlib/gem |

| json | stdlib/gem |

| llm/client | path |

| llm/enricher | path |

| llm/prompt_builder | path |

| llm/response_parser | path |

| llm/summarizer | path |

| MetricsHelper | stdlib/gem |

| net/http | path |

| pathname | stdlib/gem |

| rack/test | path |

| Rack::Test::Methods | stdlib/gem |

| rails | stdlib/gem |

| ripper | stdlib/gem |

| set | stdlib/gem |

| shellwords | stdlib/gem |

| sinatra/base | path |

| spec_helper | stdlib/gem |

| support/llm_mock_helper | path |

| tempfile | stdlib/gem |

| TemplateHelper | stdlib/gem |

| thor | stdlib/gem |

| time | stdlib/gem |

| timeout | stdlib/gem |

| tmpdir | stdlib/gem |

| transformer | stdlib/gem |

| transformer/class_hierarchy_builder | path |

| transformer/container_data_flow_builder | path |

| transformer/erd_relationship_builder | path |

| transformer/files_data_builder | path |

| transformer/graph_data_builder | path |

| uri | stdlib/gem |

| utils/markdown_helper | path |

| utils/yaml_config_loader | path |

| yaml | stdlib/gem |

| yard | stdlib/gem |



## Related Documents

- [INDEX.md](INDEX.md) — Detailed file and symbol index
- [AGENTS.md](AGENTS.md) — Agent-oriented module documentation


---

*Generated by auto-doc. Review and commit these drafts as your documentation.*
