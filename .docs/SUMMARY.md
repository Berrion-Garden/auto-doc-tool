
# SUMMARY: auto-doc-tool

> **Generated by auto-doc v1.0.0** on 2026-07-15 22:16:20 PDT

## Purpose

### Summary
**auto-doc-tool** is an automated documentation generation system designed for Ruby and Rails projects. Its purpose is to analyze source code, database schemas, and existing documentation tags to produce comprehensive, up-to-date documentation artifacts. The tool combines static code analysis with AI/LLM integration to generate text-based documentation, visual diagrams, and searchable indexes, ensuring project documentation remains synchronized with the codebase.

### Key Components

*   **Analyzer:** Inspects Ruby source files, YARD comments, database migrations, and schema definitions to extract code structure, associations, and imports.
*   **Transformer:** Converts raw analysis data into structured representations, including class hierarchies, data flow graphs, ERD relationships, and file trees.
*   **LLM Integration:** Utilizes a client and summarizer to leverage Large Language Models, likely for enhancing documentation content, generating summaries, or creating AI-agent style documentation.
*   **Generator:** Produces various documentation outputs, including:
    *   Markdown files (Readme, Agent instructions, summaries).
    *   Visual diagrams (C4 architecture, Class diagrams, ERD).
    *   Vector indexes for semantic search.
    *   Architecture and map documentation.
*   **Orchestrator:** Manages the documentation pipeline through modular steps (e.g., generating architecture, building diagrams, indexing vectors) to ensure a cohesive final output.
*   **Server & Search:** Provides a web server to serve the generated documentation and a search service to enable querying the documentation index.
*   **Reporter:** Includes tools for auditing documentation quality, checking completeness, and generating compliance reports.
*   **CLI:** Offers a command-line interface to execute the tool and configure generation tasks.
*   **Config:** Manages project settings and YAML-based configuration files.

## Key Components


| Component | Type | Summary |
|-----------|------|---------|

| auto-doc-tool | module | ### Architectural Overview
`AutoDoc` is a modular Ruby tool designed to automatically analyze, transform, and generate comprehensive documentation for Ruby/Rails projects. The architecture follows a pipeline pattern with clear separation of concerns: parsing source code, structuring internal representations, orchestrating generation steps, enriching content via LLMs, and producing multiple output formats.

### Component Relationships & Dependencies
The codebase is organized into logical layers that depend on each other in a unidirectional flow:

* **CLI & Configuration (`cli.rb`, `config.rb`, `auto_doc.rb`)**: Entry point. Loads project configuration, validates paths, and initiates the documentation pipeline.
* **Analyzer Layer (`analyzer/`)**: Responsible for raw source extraction. Components parse Ruby source code, extract YARD documentation, read database schema, map model associations, identify imports, and track diffs/cached analysis results.
* **Transformer Layer (`transformer/`)**: Consumes raw analysis data and converts it into structured, queryable formats. Builds class hierarchies, container data flows, ERD relationships, file inventories, and graph/node structures required for diagrammatic outputs.
* **Orchestrator Layer (`orchestrator/`)**: Coordinates the end-to-end workflow. Sequences discrete steps (manifest generation, vector indexing, diagram rendering, markdown generation, LLM integration) using a pipeline pattern with base step abstractions and metrics tracking.
* **Generator Layer (`generator/`)**: Output producers. Consume transformed data to render specific artifacts: C4/class/ERD diagrams, Markdown documentation, README, index files, vector embeddings, and agent-specific documentation.
* **LLM Integration (`llm/`)**: Provides abstraction for external AI models. Handles client communication and text summarization, likely used for enhancing documentation content, generating summaries, or enriching metadata.
* **Reporter & Utilities (`reporter/`, `utils/`)**: Validates output completeness and runs audits. Provides cross-cutting utilities (file tree navigation, markdown formatting, timestamp tracking, YAML config loading, output formatting) consumed by all layers.

### Data Flow Pipeline
1. **Ingestion**: The CLI targets a Ruby project directory (or test fixture). Configuration is loaded.
2. **Analysis**: Source code, migrations, schema, and YARD comments are parsed. Dependencies, associations, and imports are extracted and cached.
3. **Transformation**: Raw parsed data is normalized into internal models: class graphs, data flow maps, and relationship trees.
4. **Orchestration**: The pipeline steps execute sequentially. Each step may read from the transformed state, trigger specific generators, or request LLM processing.
5. **Enrichment**: The LLM module receives targeted text/structures to generate summaries, enhance descriptions, or produce AI-assisted documentation segments.
6. **Generation & Reporting**: Generators render final assets (diagrams, markdown, vectors). The reporter audits the output against completeness rules. Formatted results are returned or persisted.
7. **Output Delivery**: Artifacts are written to disk or served via the embedded server, ready for consumption.

### Test & Fixture Dependencies
* **Spec Suite**: Mirrors the `lib/` structure, providing unit and integration tests for every component. Specs are tightly coupled to their implementation modules.
* **Test Fixtures**: Sample Ruby and Rails projects (`sample_ruby_project`, `rails_project`, `partial_docs_project`, `test_fixtures/`) act as controlled data sources. They supply representative code, models, controllers, services, and migrations to validate parsing accuracy, association detection, and output generation across diverse project patterns.

### Key Design Characteristics
* **Pipeline-Centric**: Data moves linearly from parsing → transformation → orchestration → generation → validation.
* **High Modularity**: Generators, analyzers, and orchestrator steps are decoupled, enabling extension (e.g., new diagram types or analysis rules) without impacting the core workflow.
* **AI-Augmented**: LLM integration is isolated, allowing documentation enrichment or summarization without tightly coupling to parsing/generation logic.
* **Self-Validating**: Built-in completeness checking and audit reporting ensure generated documentation meets coverage thresholds. |



## Architecture Pattern

## Overall Architecture
`auto-doc-tool` employs a modular, pipeline-driven architecture designed for automated static analysis and multi-format documentation generation. The system is structured around a composable orchestration layer that coordinates parsing, transformation, AI-assisted generation, and validation phases. It leverages incremental analysis, caching, and vector-based indexing to efficiently process large codebases while producing both human-readable and machine-consumable documentation artifacts.

## Major Modules
- **Analyzer (`analyzer/`)**: Performs static code inspection. Extracts imports, model associations, database schema, and existing YARD documentation. Includes caching and diff detection to avoid redundant processing.
- **Transformer (`transformer/`)**: Converts raw analysis output into structured internal models. Constructs class hierarchies, container data flows, dependency graphs, ERD relationships, and file-level metadata.
- **Orchestrator (`orchestrator/`)**: Manages the end-to-end execution workflow using a step-based pipeline pattern. Coordinates sequencing, state management, and cross-module handoffs.
- **Generator (`generator/`)**: Produces final documentation outputs. Supports Markdown (including AI-agent-ready formats), architecture overviews, C4/class/ERD diagrams, READMEs, summaries, and vector embeddings.
- **LLM Integration (`llm/`)**: Interfaces with language models to handle summarization, AI-assisted content generation, and semantic query enhancement.
- **Reporter (`reporter/`)**: Validates generated documentation against coverage standards, audits completeness, and identifies documentation gaps.
- **Core & Utilities**: Provides CLI and server entry points, configuration management, a persistent documentation index, search functionality, and shared helpers for YAML parsing, markdown formatting, file tree construction, and timestamp tracking.

## Module Relationships & Data Flow
1. **Initiation**: The `CLI` or `Server` component loads project configuration and initializes the `Orchestrator::Pipeline`.
2. **Analysis Phase**: The pipeline triggers analyzer steps that scan source files, Rails components, and database schema. Results are cached and diff-checked for efficiency.
3. **Transformation Phase**: Parsed data flows into transformer builders that construct hierarchical, graph-based, and relational models of the codebase.
4. **Generation Phase**: The orchestrator delegates to generator components, which consume transformed models to produce Markdown, diagrams, vector embeddings, and agent-ready documentation. The `LLM` submodule is invoked here for summarization and AI-driven content enrichment.
5. **Indexing & Reporting**: Generated assets are persisted in a `DocumentationIndex`, enriched with search vectors, and validated by the `Reporter` module to ensure completeness and coverage standards.
6. **Cross-Cutting Support**: Utility modules provide shared functionality across all phases, while the `SearchService` enables semantic querying of the generated documentation.

## Architectural Characteristics
- **Pipeline-First Design**: Clear separation of concerns via composable orchestration steps.
- **AI-Augmented Workflows**: Dedicated LLM integration enables automated summarization, agent instruction generation, and semantic search.
- **Multi-Format Output**: Supports static documentation, visual diagrams, and machine-readable vector/embedding formats.
- **Incremental Processing**: Diff services and analysis caches optimize performance for large or frequently changing codebases.
- **Test-Driven Ecosystem**: Extensive fixture coverage (Rails, standard Ruby, partial docs) and comprehensive spec suites ensure robust parsing and generation across diverse projects.

## Dependencies Overview


| Name | Type |
|------|------|

| ../auto_doc/server | local |

| active_record | stdlib/gem |

| auto_doc | stdlib/gem |

| auto_doc/agent_query_service | path |

| auto_doc/analyzer/analysis_cache | path |

| auto_doc/analyzer/analysis_pipeline | path |

| auto_doc/analyzer/diff_service | path |

| auto_doc/analyzer/import_extractor | path |

| auto_doc/analyzer/model_association_parser | path |

| auto_doc/analyzer/orphans_service | path |

| auto_doc/analyzer/schema_parser | path |

| auto_doc/analyzer/source_parser | path |

| auto_doc/analyzer/yard_reader | path |

| auto_doc/cli | path |

| auto_doc/config | path |

| auto_doc/documentation_index | path |

| auto_doc/generator/agents_md_generator | path |

| auto_doc/generator/architecture_generator | path |

| auto_doc/generator/c4_diagram_generator | path |

| auto_doc/generator/class_diagram_generator | path |

| auto_doc/generator/diagram_generator | path |

| auto_doc/generator/erd_generator | path |

| auto_doc/generator/index_generator | path |

| auto_doc/generator/map_generator | path |

| auto_doc/generator/readme_generator | path |

| auto_doc/generator/summary_generator | path |

| auto_doc/generator/template_helper | path |

| auto_doc/generator/vector_generator | path |

| auto_doc/llm | path |

| auto_doc/orchestrator | path |

| auto_doc/orchestrator/agents_md_step | path |

| auto_doc/orchestrator/architecture_step | path |

| auto_doc/orchestrator/base_step | path |

| auto_doc/orchestrator/diagram_step | path |

| auto_doc/orchestrator/index_summary_vectors_step | path |

| auto_doc/orchestrator/manifest_step | path |

| auto_doc/orchestrator/metrics_helper | path |

| auto_doc/orchestrator/pipeline | path |

| auto_doc/orchestrator/readme_step | path |

| auto_doc/reporter/audit_reporter | path |

| auto_doc/reporter/completeness_checker | path |

| auto_doc/search_service | path |

| auto_doc/server | path |

| auto_doc/tester/e2e_runner | path |

| auto_doc/transformer | path |

| auto_doc/utils/file_tree_builder | path |

| auto_doc/utils/markdown_helper | path |

| auto_doc/utils/output_formatter | path |

| auto_doc/utils/timestamp_tracker | path |

| auto_doc/utils/yaml_config_loader | path |

| auto_doc/version | path |

| AutoDoc::Generator::TemplateHelper | stdlib/gem |

| AutoDoc::Orchestrator::MetricsHelper | stdlib/gem |

| Enumerable | stdlib/gem |

| erb | stdlib/gem |

| fileutils | stdlib/gem |

| json | stdlib/gem |

| llm/client | path |

| llm/summarizer | path |

| MetricsHelper | stdlib/gem |

| net/http | path |

| pathname | stdlib/gem |

| rack/test | path |

| Rack::Test::Methods | stdlib/gem |

| rails | stdlib/gem |

| ripper | stdlib/gem |

| set | stdlib/gem |

| shellwords | stdlib/gem |

| sinatra/base | path |

| spec_helper | stdlib/gem |

| TemplateHelper | stdlib/gem |

| thor | stdlib/gem |

| time | stdlib/gem |

| timeout | stdlib/gem |

| tmpdir | stdlib/gem |

| transformer | stdlib/gem |

| transformer/class_hierarchy_builder | path |

| transformer/container_data_flow_builder | path |

| transformer/erd_relationship_builder | path |

| transformer/files_data_builder | path |

| transformer/graph_data_builder | path |

| uri | stdlib/gem |

| utils/markdown_helper | path |

| utils/yaml_config_loader | path |

| yaml | stdlib/gem |

| yard | stdlib/gem |



## Related Documents

- [INDEX.md](INDEX.md) — Detailed file and symbol index
- [AGENTS.md](AGENTS.md) — Agent-oriented module documentation


---

*Generated by auto-doc. Review and commit these drafts as your documentation.*
